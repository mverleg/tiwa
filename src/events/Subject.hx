package events;

//todo: how to deal with different event types? observers should check that? or a different subject for each?

class Subject {
    /*
    Lets observers register to see events. 'Subject' is apparently the standard name for that, for some reason.
    Observer pattern: http://gameprogrammingpatterns.com/observer.html
    I call it observer pattern since it's synchronous without queue.
     */
    public function new() {
    }

}
