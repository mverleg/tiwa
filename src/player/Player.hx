package player;
class Player {
    /*
    Player should form the interface between the game which wants inputs,
    and whatever is playing which should provide them.
     */
    public function new() {
    }
}
